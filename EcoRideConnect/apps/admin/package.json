{
  "name": "ecoride-admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "dev": "VITE_API_URL=auto VITE_SIMPLE_AUTH=true VITE_DEFAULT_ROLE=admin vite --config vite.config.ts --host --port 5175",
    "dev:full": "VITE_API_URL=auto VITE_SIMPLE_AUTH=false VITE_DEFAULT_ROLE=admin vite --config vite.config.ts --host --port 5175",
    "build": "vite build --config vite.config.ts",
    "preview": "vite preview --port 5175 --host"
  }
}
