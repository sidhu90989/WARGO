name: (deprecated) Neon preview (removed)
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is deprecated and does nothing.'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Neon/Vercel preview workflow has been removed."
